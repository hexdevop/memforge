# Install everything needed for development and testing:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt

# Runtime dependencies
anthropic>=0.40
typer>=0.12
pydantic>=2.6
python-frontmatter>=1.1
rank-bm25>=0.2
rich>=13.7
fastapi>=0.110
python-multipart>=0.0.9
uvicorn[standard]>=0.27
jinja2>=3.1
markdown-it-py>=3.0
gitpython>=3.1
pyyaml>=6.0

# Dev / test
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.4
black>=24.0

# Install the package itself in editable mode:
# pip install -e .
